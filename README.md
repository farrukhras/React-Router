# React-Router

## Local Client App 

* Clone the repository to your local directory.

  ```bash
  git clone 
  cd src
  ```
* Install client-side dependencies and start React App
  ```bash
  cd client
  npm install
  npm start
  ```
